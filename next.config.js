// next.config.js
module.exports = {
  // Configuración mínima para evitar problemas
  reactStrictMode: true,
  // Deshabilita temporalmente la optimización que podría causar problemas
  swcMinify: false
};
